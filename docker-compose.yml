version: "3.9"

services:

  db:
    image: postgres:16

    environment:
      POSTGRES_DB: extraction_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

  web:
    build: .

    ports:
      - "8000:8000"

    depends_on:
      - db
